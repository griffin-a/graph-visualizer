const Navigation = () => {
    return (
        <nav className="navbar">
            <div className="links">
                <a href="/">Home</a>
                <a href="/grid">Grid</a>
            </div>
        </nav>
    );
}

export default Navigation;